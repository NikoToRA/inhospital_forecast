name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      
      # Pythonセットアップ
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'
      
      # 依存関係のインストール
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r frontend/requirements.txt
          pip install -r api/requirements.txt
      
      # フロントエンドのビルド
      - name: Build frontend
        run: |
          mkdir -p frontend/.output
          cd frontend
          echo '<!DOCTYPE html><html><head><title>InHospital Forecast</title></head><body><h1>InHospital Forecast API</h1><p>API endpoints available at:</p><ul><li><a href="/api/DataFunction">/api/DataFunction</a></li><li><a href="/api/PredictFunction">/api/PredictFunction</a></li></ul></body></html>' > .output/index.html
          cd ..
      
      # デバッグ情報
      - name: Debug environment
        run: |
          echo "Current directory structure:"
          find . -type d -not -path "*/\.*" | sort
          echo "Content of frontend/.output:"
          ls -la frontend/.output
      
      # Static Web Appsへのデプロイ
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "frontend"
          api_location: "api"
          output_location: ".output"
          skip_app_build: true

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
          app_location: "frontend" 